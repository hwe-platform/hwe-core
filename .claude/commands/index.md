Los comandos reales están en docs/.claude/commands/ (submodule hwe-tools).
Los archivos en esta carpeta son stubs que cargan el comando real.
Para añadir un nuevo comando, crear el archivo en hwe-tools y un stub aquí.
