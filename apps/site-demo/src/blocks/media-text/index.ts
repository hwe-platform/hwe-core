/**
 * Imagen + texto de este site.
 *
 * Reexporta el de plataforma. Lo propio de La Civelle no está en el bloque sino
 * en lo que se le pasa por los slots: la insignia «Depuis 30 Ans» va en
 * `sobreLaImagen`, y el bloque de plataforma sigue sin saber que existe.
 */
export { MediaTextBlock } from '@hwe-platform/core-ui';
export { Insignia } from './Insignia';
